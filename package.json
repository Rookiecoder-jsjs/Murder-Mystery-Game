{
  "name": "murder-mystery",
  "private": true,
  "version": "1.0.0",
  "description": "AI-powered murder mystery game — root launcher",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:backend": "node scripts/dev-backend.js",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:miniprogram": "cd miniprogram && npm run dev:weapp",
    "build:miniprogram": "cd miniprogram && npm run build:weapp",
    "install:all": "npm install && cd frontend && npm install && cd ../miniprogram && pnpm install --frozen-lockfile",
    "test:backend": "cd backend && python -m pytest"
  }
}
